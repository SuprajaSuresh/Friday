<%= form_for([@user, @user.posts.build]) do |f| %>
    <div>
      <%= f.label :Title %><br />
      <%= f.text_field :title%>
    </div>
    <div>
      <%= f.label :Content %><br />
      <%= f.text_area :content %>
    </div>
    <div>
      <%= f.submit %>
    </div>
<% end %>
